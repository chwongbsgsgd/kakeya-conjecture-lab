# 挂谷猜想实验室 - 学习流程记录

## 一、项目背景

本项目复现并扩展了 github.com/tjxj/kakeya-conjecture-lab，这是一个关于挂谷猜想的互动科普网站。

**核心目标**：建立一套可复用的"数学/科学问题 → 互动科普页面"的流程模板，并基于深入学习的知识进行创新扩展。

**创新扩展**：在原始项目基础上，添加了三个新的实验模块，将挂谷猜想的数学理论与实际应用更紧密地结合。

---

## 二、完整流程

### 步骤 1：事实核验（Verification）

**目的**：确保所有数学事实准确无误，区分不同概念，追溯到可靠来源。

**操作清单**：

1. **区分三个问题**：
   - 挂谷针问题（1917）：平面内转针所需最小面积
   - Besicovitch 集合（1928）：包含每个方向单位线段的集合，可零测度
   - 现代挂谷集合猜想：Hausdorff/Minkowski 维数必须等于空间维数

2. **准确说明关键结论**：
   - 三维情形：王虹与 Zahl 在 2025 年证明，两种维数都等于 3
   - 四维及以上：仍然开放
   - Kakeya maximal conjecture：仍未解决
   - 菲尔兹奖：2026 年王虹获奖，三维挂谷猜想是代表性成果之一

3. **追溯来源**：
   - 官方资料：ICM 2026 官网、菲尔兹奖官方获奖词
   - 原始论文：Wang & Zahl (2025), Wang & Zahl (2026), Guth et al. (2026)
   - 可靠综述：Wolff (2000), Guth (2015)

4. **明确边界**：
   - 有限线段、有限细管、像素覆盖和动画只用于建立直觉
   - **不能充当数学证明**

---

### 步骤 2：直觉模型设计（Intuition Modeling）

**目的**：将抽象数学概念转化为可操作的视觉模型。

**设计原则**：

1. **从简单到复杂**：先理解二维直觉，再推广到三维
2. **可交互性**：用户可以通过拖动、调参数来探索
3. **明确局限性**：清楚告知用户模型的边界

**本项目的直觉模型**：

#### 模型 A：三维针旋转（Three.js）

- 展示多根针在三维空间中旋转
- 用户可以暂停/继续旋转
- 直观感受"每个方向都有一根针"

#### 模型 B：二维覆盖实验（Canvas）

- 有限方向的单位线段
- 三种模式：中心交汇、错位压缩、分组束化
- 用户可调方向数量、压缩程度
- 实时显示覆盖比例

#### 模型 C：δ 邻域可视化（Canvas）

- 展示线段的 δ 邻域
- 用户可调 δ 值
- 理解离散细管与连续概念的关系

#### 模型 D：Kakeya 维数估计（Canvas）

- 生成不同类型的结构（真3D、薄板、针状、挂谷集合）
- 计算方向梯度能量分布
- 估计 Kakeya 维数和方向能量不均衡度
- 理解维数估计如何区分不同类型的结构

#### 模型 E：多尺度颗粒化采样（Canvas）

- 基于颗粒性分解技术的自适应采样策略
- 三种采样模式：均匀采样、自适应采样、方向敏感采样
- 可视化采样点分布
- 计算采样点数、信息增益和覆盖率

#### 模型 F：波包分解（Canvas）

- 基于波包分解技术的自适应多分辨率重建
- 可调尺度数量和方向数量
- 三种显示模式：合成结果、单独波包、能量分布
- 计算波包总数、高频能量占比和方向均匀度

---

### 步骤 3：互动设计（Interaction Design）

**目的**：设计用户与直觉模型的互动方式。

**互动模式**：

| 互动类型 | 实现方式 | 学习目标 |
|---------|---------|---------|
| 导航切换 | 顶部导航栏 + 移动端菜单 | 在不同概念之间切换 |
| 暂停/继续 | 按钮控制旋转状态 | 观察特定状态 |
| 参数调节 | 滑块控制方向数、压缩度、δ值 | 探索参数影响 |
| 模式切换 | 下拉菜单选择不同算法 | 对比不同构造方法 |
| 分层查看 | 直觉层/数学层切换 | 从直观到精确 |

---

### 步骤 4：科学边界说明（Boundary Clarification）

**目的**：明确告知用户互动实验与数学证明的区别。

**边界声明**：

1. **首页声明**：
   > 有限线段、有限细管、像素覆盖和动画只用于建立直觉，不能充当数学证明

2. **证明地图说明**：
   > 文字保持克制，不声称网页展示了完整证明

3. **定义区说明**：
   > δ 邻域可视化使用离散细管模拟连续概念

4. **页脚声明**：
   > 本页面仅用于科普和直觉建立，不构成数学证明

---

### 步骤 5：页面结构设计（Page Structure）

**目的**：设计清晰的叙事结构，引导用户逐步理解。

**页面结构**：

1. **首屏（直觉）**：大标题 + 三维实验台 + 开始按钮
2. **三个问题**：区分挂谷针问题、Besicovitch 集合、现代猜想
3. **二维实验**：可交互的覆盖实验
4. **正式定义**：精确的数学定义 + δ 邻域可视化
5. **证明地图**：证明的核心概念，支持直觉层/数学层切换
6. **时间线**：从 1917 到 2026 的发展历程 + 开放问题
7. **Kakeya 维数估计**：基于方向梯度能量分布的维数估计方法
8. **多尺度颗粒化采样**：基于颗粒性分解技术的自适应采样策略
9. **波包分解**：基于波包分解技术的自适应多分辨率重建
10. **来源**：所有结论的可靠来源

---

### 步骤 6：响应式设计（Responsive Design）

**目的**：确保电脑和手机都能良好访问。

**响应式策略**：

| 屏幕尺寸 | 布局策略 |
|---------|---------|
| >768px | 网格布局，双栏展示 |
| 480-768px | 单列布局，简化导航 |
| <480px | 单列布局，精简内容 |

**关键适配**：

- 导航栏：桌面端水平菜单，移动端汉堡菜单
- 三维实验台：高度自适应
- 时间线：桌面端左右交替，移动端单列
- 控制按钮：保持可点击尺寸（最小 44px）

---

### 步骤 7：技术实现（Technical Implementation）

**技术栈**：

- 纯静态 HTML/CSS/JavaScript
- Three.js（三维可视化）
- Canvas（二维实验）
- CDN 引入依赖（避免构建工具兼容性问题）

**项目结构**：

```
kakeya-conjecture-lab/
├── index.html      # 主页面
├── style.css       # 样式
├── main.js         # 交互逻辑
├── package.json    # 项目配置
└── LEARNING_PROCESS.md  # 学习流程记录
```

**开发命令**：

```bash
npm run dev    # 启动开发服务器（python3 -m http.server 5173）
npm run build  # 构建（静态站点，无需构建）
npm run preview # 预览（同dev）
```

---

### 步骤 8：发布部署（Deployment）

**部署方式**：

1. **静态托管**：将所有文件上传到静态网站托管服务
   - GitHub Pages
   - Netlify
   - Vercel
   - 任何支持静态文件的服务器

2. **单文件发布**：
   - HTML 内嵌 CSS 和 JavaScript（可选）
   - 生成单一 HTML 文件，便于分享

**发布检查清单**：

- [ ] 页面在桌面端正常显示
- [ ] 页面在移动端正常显示
- [ ] 三维可视化正常工作
- [ ] 二维实验正常交互
- [ ] 所有链接正常
- [ ] 边界声明清晰可见

---

## 三、可复用模板

### 模板结构

```
project-name/
├── index.html      # 主页面
├── style.css       # 样式
├── main.js         # 交互逻辑
├── package.json    # 项目配置
└── LEARNING_PROCESS.md  # 学习流程记录
```

### 模板流程（适用于其他数学/科学问题）

1. **事实核验**：查清问题的历史、定义、关键结论、开放问题
2. **直觉模型设计**：设计可可视化的模型
3. **互动设计**：设计用户与模型的互动方式
4. **边界说明**：明确模型与真实科学的区别
5. **页面结构**：设计清晰的叙事流程
6. **响应式设计**：确保多设备兼容
7. **技术实现**：使用纯静态技术，避免复杂构建
8. **发布部署**：发布到静态托管服务

### 模板提示词（用于 AI 生成）

```
你是一名擅长科学可视化、交互叙事和前端开发的设计工程师

请为我制作一个中文单页互动科普网站

项目主题：[替换为具体数学/科学问题]

项目目标：让第一次接触该主题的普通读者先建立直觉，再理解正式定义、关键结论与仍未解决的问题

页面必须适合电脑和手机访问

成品要有完整视觉设计，避免普通文档页和模板页的感觉

一、事实与资料要求
1. 先核验资料再写页面
2. 区分核心概念和相关问题
3. 准确说明关键结论、证明者、时间
4. 所有重要结论都要能回到官方资料、原始论文或可靠综述
5. 禁止虚构定理、数字、引文、论文结论和证明步骤
6. 页面必须明确提示：可视化和动画只用于建立直觉，不能充当数学证明

二、页面叙事结构
1. 顶部导航（直觉、定义、证明/关键概念、时间线、来源）
2. 首屏（大标题、简短说明、开始实验按钮、主要可视化）
3. 核心概念区分
4. 直觉实验（可交互）
5. 正式定义
6. 证明地图/关键概念
7. 时间线
8. 开放问题
9. 来源

三、技术要求
1. 使用纯静态 HTML/CSS/JavaScript
2. 三维可视化使用 Three.js（CDN引入）
3. 二维可视化使用 Canvas
4. 响应式设计，支持移动端
5. 无需复杂构建工具
```

---

## 四、关键学习经验

### 经验 1：事实核验优先

在写任何代码之前，必须先核实所有数学事实。错误的事实会导致整个项目失去价值。

### 经验 2：直觉模型的边界

明确告知用户直觉模型的局限性非常重要，避免用户误解为数学证明。

### 经验 3：纯静态技术的优势

使用纯静态技术（HTML/CSS/JS + CDN）可以：
- 避免构建工具兼容性问题
- 简化部署流程
- 提高加载速度
- 便于分享和存档

### 经验 4：响应式设计的重要性

确保手机用户也能良好体验，是科普网站成功的关键。

### 经验 5：叙事结构的设计

从简单到复杂、从直观到精确的叙事顺序，有助于用户逐步理解抽象概念。

---

## 五、扩展方向

### 扩展 A：更多互动实验

- 添加更多参数调节
- 添加数据导出功能
- 添加对比模式

### 扩展 B：更多数学概念

- 添加 Hausdorff 维数的计算演示
- 添加 Minkowski 维数的计算演示
- 添加多项式分割的可视化

### 扩展 C：国际化支持

- 添加英文版本
- 添加多语言支持

### 扩展 D：离线版本

- 打包为桌面应用（Electron）
- 打包为 PWA

---

## 六、参考文献

1. Wang, H. & Zahl, J. (2025). Volume Estimates for Unions of Convex Sets, and the Kakeya Set Conjecture in Three Dimensions. arXiv:2502.17655
2. Wang, H. & Zahl, J. (2026). Sticky Kakeya Sets and the Sticky Kakeya Conjecture. JAMS
3. Guth, L., Wang, H., & Zahl, J. (2026). A Streamlined Proof of the Kakeya Set Conjecture in R^3
4. 互动实验原始项目：https://github.com/tjxj/kakeya-conjecture-lab